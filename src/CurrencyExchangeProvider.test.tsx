import { getExchangeRate, getChart } from './CurrencyExchangeProvider';

xtest('gets the Exchange Rate from API', async () => {
  // Mocking API calls should be here
  // Test placeholder
});

xtest('gets the Exchange Rate from cache', async () => {
  // Test placeholder
});

xtest('fetches the Exchange Rate', async () => {
  // Test placeholder
});


xtest('gets the Chart Data from API', async () => {
  // Mocking API calls should be here
  // Test placeholder
});

xtest('gets the Chart Data from cache', async () => {
  // Test placeholder
});

xtest('fetches the Chart Data', async () => {
  // Test placeholder
});
